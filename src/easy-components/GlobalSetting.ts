export const GolobalPaginationSetting = {
  showQuickJumper: true,
  showSizeChanger: false,
  showTotal: (total: number /* range: [number, number] */) => `共 ${total} 条记录`,
};

export const GolobalSearchFormLayout = {
  md: 12,
  xl: 8,
  xxl: 6,
};
